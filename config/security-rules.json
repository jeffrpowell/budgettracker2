{
  "rules": {
    ".read": true,
    ".write": true,
	"filterDate": {
      ".read": "auth.uid !== null",
      ".write": "auth.uid !== null",
	  ".validate": "newData.isNumber() && newData.val() > 201401 && newData.val() < 202012"
    }
  }
}